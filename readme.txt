1) Install node.js (If nessesary)

2) BUILD Angular sample: 
	2.1) install Angular CLI: npm install @angular/CLI (If nessesary)
	2.2) Build Angular sample: npm serve
   OR	
    RUN Angular sample: folder app-angular/dist		


3) BUILD VUE sample: 
	2.1) install VUE CLI: npm install -g @vue/cli (If nessesary)
	2.2) Build VUE sample: npm run serve
   OR	
    RUN VUE sample: folder app-vue/dist		
 